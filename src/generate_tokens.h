#ifndef GENERATE_TOKENS
#define GENERATE_TOKENS

void generate_tokens(int rank, int max_tokens, int n, int m, int h, int V, Vocab* vocab, double* C, double* H, double* d, double* U, double* b);

#endif